name: Build & Push ADMIN (dev)

on:
  push:
    branches:
      - feature/v1.1.0

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: usukhbaya12/hire-admin

jobs:
  # №9-L0 / №15-P1: улаан тест image-ийг гаргахгүй. Тестүүд зөвхөн Node (npm install шаардахгүй):
  # api-boundary (browser core-г шууд дуудахгүй), exam-answers / exam-resume / exam-actions (web).
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm test

  build:
    needs: test
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push ADMIN
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:feature-v1.1.0
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
